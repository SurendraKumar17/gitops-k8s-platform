apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-payment
  labels:
    {{- include "booking.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: payment
  ports:
    - name: http
      port: 3003
      targetPort: http
      protocol: TCP