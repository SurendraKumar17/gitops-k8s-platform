apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-notification
  labels:
    {{- include "booking.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: notification
  ports:
    - name: http
      port: 3004
      targetPort: http
      protocol: TCP